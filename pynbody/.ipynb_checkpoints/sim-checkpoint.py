"""
This submodule defines a "sim" class, which can be used to perform analyses
over a set of snapshots from a given simulation (for example plotting certain properties 
of a halo (like central density) over time).
"""

class sim:
    def __init__(self, path)